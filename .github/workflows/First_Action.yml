name: First Action
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo " Hello World "
      - name: Install requirements.txt
        run: python -r requirements.txt
